In order to run the proc.c program in its 'verbose mode', you must pass only one argument.

Here is some command-line code to clarify:

gcc proc.c
./a.out 5

Once the above code has executed, the proc.c program should now be running in the 'verbose mode'.
